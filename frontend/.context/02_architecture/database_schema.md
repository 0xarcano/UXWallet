# Database Schema

*(N/A — Client-side state only.)*

| Store | Description |
|-------|-------------|
| **LocalStorage** | User preferences, theme, and cached session metadata. |
| **Nitrolite Session** | In-memory off-chain state for the current clearing session. |
